import { keyframes } from '@emotion/react';
import {
  fadeInUp as _fadeInUp,
  fadeInLeft as _fadeInLeft,
  fadeInRight as _fadeInRight,
  bounceInDown as _bounceInDown
} from 'react-animations';

export const fadeInUp = keyframes`${_fadeInUp}`;
export const fadeInLeft = keyframes`${_fadeInLeft}`;
export const fadeInRight = keyframes`${_fadeInRight}`;
export const bounceInDown = keyframes`${_bounceInDown}`;
